# Optional. If unset, the report uses a local writer instead of Grok.
XAI_API_KEY=

# Postgres (Neon works). Required on Netlify so the partner inbox persists.
# Without this, serverless instances use an in-memory database that resets.
DATABASE_URL=

# Keep auth off for this app.
VITE_AUTH_ENABLED=false
